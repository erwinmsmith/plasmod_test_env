# Dataset Manifests

Put small dataset manifests here.

A manifest should record the dataset or trace name, generator version, generation parameters, session count, event count, query count, payload-size distribution, visibility modes, embedding ratio, file paths, and checksums when available.
