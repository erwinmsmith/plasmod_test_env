# Synthetic Agent Event Stream

Put generated synthetic event streams here.

Use this source for controllable pressure tests such as write-to-visible latency, materialization lag, stale result rate, latest-state hit rate, and retrieval latency under write load.

Keep generated `.jsonl`, `.csv`, `.parquet`, and other large files untracked unless a small fixture is intentionally added for tests.
